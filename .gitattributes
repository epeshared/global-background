# Default: auto-detect
* text=auto

# Shell scripts must keep LF (macOS/Linux)
*.sh text eol=lf
*.command text eol=lf
macos/launchd/*.plist text eol=lf

# PowerShell / Windows batch keep CRLF
*.ps1 text eol=crlf
*.cmd text eol=crlf
